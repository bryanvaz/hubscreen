require 'spec_helper'

describe Hubscreen do
  it 'has a version number' do
    expect(Hubscreen::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
